# 🎯 AI-läsbart systemregister för Actor Showcase

main:
  syfte: "FastAPI-applikation som hanterar all logik och returnerar strukturerad data för UI"
  input: "HTTP requests till /actors, /stats, /actors/country/{country} endpoints"
  output: "Strukturerade JSON responses med success/error states och formaterad data"
  trigger: "make api - startar uvicorn server"
  anrop: "http://localhost:8000/actors för att hämta alla skådespelare med metadata"

services/actors:
  syfte: "Business logic för skådespelardata - validering, filtrering, statistik och dataformatering"
  input: "Funktionsanrop från API endpoints med parametrar"
  output: "Validerade Actor-objekt, filtrerade listor, statistik och strukturerad data"
  beroenden: "models/actor för datastruktur"
  anrop: "get_actors(), get_actor_by_id(id), get_actors_by_country(country), get_oscar_winners(), get_actor_stats()"

models/actor:
  syfte: "Pydantic-modell som definierar datastruktur för skådespelare - typmaster för hela systemet"
  input: "Dictionary med skådespelardata (id, name, birth_year, country, oscars)"
  output: "Validerat Actor-objekt med typning och OpenAPI-metadata"
  format: "Pydantic BaseModel med Field-validators och OpenAPI-schema"
  test: "Automatisk validering vid skapande av Actor-objekt"
  trigger: "make types - genererar TypeScript från OpenAPI"

ui/src/App:
  syfte: "Pure presentation - visar skådespelare från API utan business logic"
  input: "Strukturerad data från backend API responses"
  output: "Rendering av ActorCard-komponenter med loading/error states"
  beroenden: "ActorCard-komponent, useActors hook för data fetching"
  trigger: "npm run dev i ui-mappen"

ui/src/hooks/useActors:
  syfte: "Pure data fetching - hämtar data från API utan logik"
  input: "API endpoint calls till backend"
  output: "Raw data för UI-komponenter"
  beroenden: "lib/api för fetch-anrop, genererade types från OpenAPI"
  format: "React hook som returnerar actors, loading, error states"

ui/src/lib/api:
  syfte: "Pure data fetching layer - ingen business logic"
  input: "API endpoint URLs"
  output: "Raw JSON responses från backend"
  format: "Simple fetch wrapper utan transformation"

ui/src/components/ActorCard:
  syfte: "Pure presentation component - visar skådespelardata som den kommer från API"
  input: "Actor-objekt från backend (genererade types från OpenAPI)"
  output: "Formaterad card-komponent med skådespelarinformation"
  beroenden: "shadcn/ui Card, Badge komponenter"
  format: "TypeScript types genererade från Pydantic via OpenAPI"

ui/src/types/api:
  syfte: "Genererade TypeScript types från OpenAPI - synkroniserade med Pydantic"
  input: "OpenAPI schema från FastAPI (genererat från Pydantic)"
  output: "TypeScript interfaces för alla API responses och modeller"
  beroenden: "openapi-typescript för generering"
  trigger: "make types-generate - genererar från OpenAPI"
  format: "Automatiskt genererade types från backend Pydantic-modeller"

streamlit:
  syfte: "Streamlit demo - pure presentation från samma API som React-frontend"
  input: "HTTP requests till backend API endpoints"
  output: "Streamlit UI med tabeller och statistik från API"
  beroenden: "requests för API-anrop, pandas för data-visning"
  trigger: "make streamlit - startar Streamlit på port 8501"
  format: "Pure presentation utan business logic - visar samma data som React"

Makefile:
  syfte: "Orchestrerar projektets kommandon för setup, körning, dokumentation och type-generering"
  input: "make-kommandon (help, api, ui, setup, test, docs, types, streamlit)"
  output: "Feedback med emojis och startar relevanta processer"
  trigger: "make help visar alla tillgängliga kommandon"
  anrop: "make api startar backend, make ui startar frontend, make types genererar TypeScript, make streamlit startar demo"

setup.sh:
  syfte: "Automatiserar installation av dependencies och skapar frontend-struktur"
  input: "Körning i WSL-miljö från projektets rot"
  output: "Installerad backend och frontend med alla dependencies"
  trigger: "bash setup.sh från projektets rotmapp"
  test: "Kontrollerar att main.py finns och att npm/uv är tillgängliga" 